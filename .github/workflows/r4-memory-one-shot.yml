name: R4 In-Memory Portfolio Audit One-Shot

on:
  push:
    branches: ["reconstruction/r4-portfolio-health"]
    paths: [".r4memory/TRIGGER"]

permissions:
  contents: write

jobs:
  apply-repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact R4 branch
        uses: actions/checkout@v5
        with:
          ref: reconstruction/r4-portfolio-health
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Apply reviewed repair
        run: python .r4memory/apply.py

      - name: Compile repaired tree
        run: python -m compileall -q src tests scripts

      - name: Commit self-cleaned repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(r4): detach and atomically validate in-memory portfolio state"
          git push origin HEAD:reconstruction/r4-portfolio-health
